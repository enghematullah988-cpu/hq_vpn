# HQ VPN client template — replace every placeholder with real values.
# Keep the PrivateKey secret. Never commit a real client private key to Git.
[Interface]
PrivateKey = CLIENT_PRIVATE_KEY
Address = 10.66.0.2/32
DNS = 1.1.1.1, 1.0.0.1

[Peer]
PublicKey = HQ_SERVER_PUBLIC_KEY
AllowedIPs = 0.0.0.0/0, ::/0
Endpoint = YOUR_SERVER_HOSTNAME_OR_IP:51820
PersistentKeepalive = 25
