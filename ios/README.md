# iOS platform placeholder for FlutLab import

This directory is included so Flutter project import validation recognizes the project as a multi-platform Flutter app.

The production iOS WireGuard client requires an Xcode-generated Runner target, Network Extension target, App Group `group.com.hqvpn.app`, and Apple signing/team configuration. Those account-specific settings cannot be embedded in a generic ZIP.
