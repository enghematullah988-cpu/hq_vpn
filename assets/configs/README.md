# WireGuard profiles

Do not put real production `.conf` files in this folder. HQ VPN stores imported client
profiles in platform secure storage.

Use `server/peer_template.conf` as a starting point only.
