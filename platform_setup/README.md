# Platform setup

The platform runner folders are intentionally generated by your installed Flutter SDK
so Gradle/Xcode project versions match your local Flutter release.

From the project root:

- macOS/Linux: `bash scripts/bootstrap_platforms.sh`
- Windows: `scripts\\bootstrap_platforms.ps1`

This prevents shipping stale Gradle/Xcode boilerplate that can break on a newer Flutter SDK.
