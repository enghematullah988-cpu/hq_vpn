# FlutLab import

Upload this ZIP as a new Flutter project. For Android APK choose `android-arm64` for most modern phones.

If FlutLab asks for a Web target, do not use it for the VPN runtime; Web cannot provide a native WireGuard tunnel.
